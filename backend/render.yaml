services:
  - type: web
    name: kij-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
```

**B. Make sure your backend has python-dotenv**

Check if `python-dotenv` is in your `backend/requirements.txt`. If not, add it:
```
fastapi==0.115.0
uvicorn[standard]==0.30.0
psycopg2-binary==2.9.9
pydantic==2.9.0
python-dotenv